/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Environment variables are loaded from .env files automatically
  // No need to explicitly pass them through next.config.js
}

module.exports = nextConfig
